---
classes:
  java.lang.BootstrapMethodError:
    dev_mode: false
    doc_string: |
      \brief Error thrown when the `invokedynamic` instruction inside a JVM fails to find its bootstrap method or the bootstrap method is incorrect.
    inherit_constructors: true
